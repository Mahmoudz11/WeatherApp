import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

type Props = {}

const ButtomTabs = (props: Props) => {
  return (
    <View>
      <Text>ButtomTabs</Text>
    </View>
  )
}

export default ButtomTabs

const styles = StyleSheet.create({})
